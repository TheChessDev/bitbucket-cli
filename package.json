{
  "name": "bitbucket-cli",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "clone": "bun run index.ts clone",
    "pipeline:retry": "bun run index.ts run-pipeline",
    "pipeline:create": "bun run index.ts create-pipeline",
    "pipeline:status": "bun run index.ts pipeline-status"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/inquirer-autocomplete-prompt": "^3.0.3"
  },
  "peerDependencies": {
    "typescript": "^5.6.2"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "fuse.js": "^7.0.0",
    "inquirer": "8",
    "inquirer-autocomplete-prompt": "^3.0.1",
    "ora": "^8.1.0",
    "simple-git": "^3.27.0",
    "winston": "^3.15.0"
  }
}
